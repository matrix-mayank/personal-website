{
  "scripts": {
    "serve": "bash bin/jekyll-serve",
    "build": "bash bin/jekyll-build",
    "clean": "rm -rf _site .jekyll-cache"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.0",
    "prettier": "^3.8.0"
  }
}
